apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-190311-t1tt-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-190311-t1tt.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-190311-t1tt-service
            port:
              number: 80
